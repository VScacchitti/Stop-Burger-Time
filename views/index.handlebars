<h1>STOP! Burger-Time</h1>

<h2>Time to devour the burgers!</h2>

<ul>
    {{#each burger}}
    {{#unless }}
    {{> burger/burger-block eaten= true}}
    {{/unless}}
    {{/each}}
</ul>

<h2>Cats that are eaten!</h2>

<ul>
    {{#each burger}}
    {{#if eaten}}
    {{> burger/burger-block eaten= false}}
    {{/if}}
    {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

    <div class="form-group">
        <label for="burger-name">Burger Name:</label>
        <input type="text" id="burger" name="name">
    </div>

    <div class="form-group">
        <label for="slee">Eaten?</label><br>
        <input type="radio" name="eaten" value="1" checked> Eaten?<br>
        <input type="radio" name="eaten" value="0"> Not Eaten?
    </div>

    <button type="submit">Add Burger</button>
</form>